# Example environment variables
# Do NOT commit real secrets. Add the real token in Vercel or your local .env.local

# Vercel private blob token used by the image proxy
# Add the real token in Vercel (do NOT commit a secret here)
VERCEL_BLOB_TOKEN=your_vercel_blob_token_here
